export const defaultConfig = {
    caseSensitive: false,
    exactMatch: false,
    ignoreDiacritics: true,
};

export const regexpIgnoreDiacritics = /\p{Diacritic}/gu;

export const defaultTheme = {
  color: 'marktext',
  background: 'mark',
  fontWeight: 'bold',
};
